[package]
name = "adocpdf-wasm"
description = "WASM/WASI delivery. A placeholder in this change, present so the guard constrains it from the start."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]

[lints]
workspace = true
